ServerEvents.tags("item", (event) => {
  //più item
  event.get("curios:ring").add([
      "actuallyadditions:ring_of_magnetizing",
      "actuallyadditions:ring_of_growth",
    ]);

  event.get("curios:trinket").remove
});
